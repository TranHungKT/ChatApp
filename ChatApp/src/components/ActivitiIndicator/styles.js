import { StyleSheet } from 'react-native';

import { Color } from '@common';

export default StyleSheet.create({
	activityIndicatorWrapper: {
		width: '100%',
		height: '100%',
		justifyContent: 'center',
		position: 'absolute',
		top: 0,
		// backgroundColor: Color.activeTintColor,
	},
});
